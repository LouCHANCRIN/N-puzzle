#include "../include/n_puzzle.h"

void    main(int ac, char **av)
{
    int     a;
    int     racine_len_av;
    char    **plateau;

    if (ac != 2)
    {
        ft_putstr("Nombre d'arguments invalide")
    }
    

    racine_len_av = 3;

    if (!(plateau = (short**)malloc(sizeof(short*) * racine_len_av)))
        return (0);
    return (0);
}